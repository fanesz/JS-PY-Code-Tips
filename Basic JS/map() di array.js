const numbers = [1, 2, 3, 4, 5]
const newArr = numbers.map(
    (number) => number * 2 //kali 2 untuk semua element di array
)
console.log(newArr);
// [ 2, 4, 6, 8, 10 ]

// mungkin bisa pakai for loop, tapi ini lebih simple
